{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window capabilities — minimal surface for SQL, dialog save (CSV export), and the updater. Shell/process/fs removed (not used by the renderer).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default",
    "sql:allow-load",
    "sql:allow-select",
    "sql:allow-execute",
    "dialog:default",
    "dialog:allow-save",
    "updater:default"
  ]
}
